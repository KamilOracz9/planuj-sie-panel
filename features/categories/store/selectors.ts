// import { createSelector } from "@reduxjs/toolkit";
// import { Category } from "../types";

// export const categorySelectItemsSelector = createSelector(
//     state => state.category.categoriesSelect,
//     (categoriesSelect) => categoriesSelect.map((c: Category) => ({ id: String(c.id), name: c.name }))
// );